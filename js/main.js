/* ============================================================
   main.js — Punto de entrada: inicializa todos los módulos
   ============================================================ */

initNav();
initParticles();
initAnimations();
initForm();
